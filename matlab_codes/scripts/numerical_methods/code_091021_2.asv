clear; clc; close all;

% Description: Corrects a given code

fun = @root;
x0 = 0;
n = 0:0.01:10;
x = fzero(fun,x0)

function F = root(x)
    n = 0:0.1:10
    y = n + 1
    F = ((x.^(y)-1)/(x-1)) - 1 - (
end
